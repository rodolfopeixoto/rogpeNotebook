<%= form_for(phone) do |f| %>
  <% if phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-field">
    <%= f.label :phone %>
    <%= f.text_field :phone %>
  </div>

  <div class="input-field">
    <%= f.label :contact_id %>
    <%= f.text_field :contact_id, class: 'input-field' %>
  </div>

  <div class="actions">
    <%= f.submit class: 'waves-effect waves-light btn' %>

    <%= link_to t('buttons.back'), phones_path, class: 'waves-effect waves-light btn' %>
  </div>
<% end %>
